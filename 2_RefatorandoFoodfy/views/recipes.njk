{% extends "layout.njk" %}

{% block head %}
  <head>
    <title>Foodfy - Receitas</title>
  </head>
{% endblock %}

{% block content %}

  <main>

    <div class="recipes">
      {% for item in items %}
      <div class="recipe" id="{{item.index}}">
        <img src="{{item.image}}" alt="Imagem de {{item.title}}">

        <div class="info-card">
          <strong>{{item.title}}</strong>
          <p>por {{item.author}}</p>
        </div>
      </div>
      {% endfor %}

      {% for item in items %}
      <div class="recipe" id="{{item.index}}">
        <img src="{{item.image}}" alt="Imagem de {{item.title}}">

        <div class="info-card">
          <strong>{{item.title}}</strong>
          <p>por {{item.author}}</p>
        </div>
      </div>
      {% endfor %}

    </div>
  </main>

{% endblock %}